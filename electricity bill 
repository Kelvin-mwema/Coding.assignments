//program to calculate electricity bills 
/*
program to calculate electricity  bills 
Author Mutua Kelvin Mwema 
Reg no. CT101/G/24268/24
*/
int main(){
#include<stdio.h>
char customer_name;
int customer_id,units_consumed;
float charge_per_unit,total_billed,surcharged,minimum_bill =100,total_amount_to_pay;
printf ("Enter customer name: ");
scanf ("%s",&customer_name);

printf ("Enter customer id: ");
scanf ("%d",&customer_id);

printf ("Enter units consumed: ");
scanf ("%d",&units_consumed);
 
if(units_consumed<200)
  {
   charge_per_unit=1.20;
   total_billed=charge_per_unit*units_consumed;
   total_amount_to_pay=total_billed;
  }
else if(units_consumed>=200 && units_consumed<400)
  {
   charge_per_unit=1.50;
   total_billed=charge_per_unit*units_consumed;
   total_amount_to_pay=total_billed;
  }
else if(units_consumed>=400 && units_consumed<600)
  {
   charge_per_unit =1.80;
   total_billed=charge_per_unit*units_consumed;
   total_amount_to_pay=total_billed;
  }
else if(units_consumed>=600)
  {
   charge_per_unit=2.0;
   total_billed=units_consumed*charge_per_unit;
   total_amount_to_pay=total_billed;
  }

if(total_amount_to_pay<100)
{
 total_amount_to_pay=minimum_bill;
}
if(total_amount_to_pay>400)
{
 surcharged=1.15*total_billed;
 total_amount_to_pay=surcharged;
}
printf ("Total amount to pay=%.2lf",total_amount_to_pay);
return 0;
}